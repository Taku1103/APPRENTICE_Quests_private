# フロント
## TOP
- ○ゲームのサムネ表示。特にトップ

- （話し合う）Dailyのプレイリスト、期間が欲しい
  - 開始日時と終了日時情報付与バックエンド送信、フロント描画
- clip感覚の距離つまっている
- Daily Weekly Monthlyは個人的に左寄せたい
- （駒さんやってる）Topの描画されるPlaylist欄、名前がプレイリスト感出したい。

- playlist,clipsの名前変更

- ○クリップの視聴回数表示
- ○game_title表示
- when?
- 再生時間をクリップの前に出す


- ○境目線
- ○一覧？
- ＜＞

## Header
- 描画が動的に変化しない
- user playlistsなのでマイプレイリスト一覧といったプレイリストの複数形の意味合いを持たせた言葉じゃないと不自然

## Sidebar
- sidebarのタブボタンもpointerに
- 同期的に描画しない
  - favorite押したのに更新されるまでない、playlist作成押したのに
- ログインしていないのに右にtestuserが描画される
  - 名前はuserのdisplay_nameのはず
  - アイコンはprofile画像

## Playlist
- playlist内のclip解除（フロント描画処理、バックにupdate送信）
- ハートマークの位置は動画の方に近く
- 前へ、次へをもう少し大きく、場所も洗練させたい
- playlistのプレイリスト追加周りハンバーガーメニューorボタン化

# バック
## user
<!-- - (駒さんがやる)なんか梶さんがやってほしいとこと
  - ログインのユーザー情報の表示(本来はshowアクションの挙動
  - 何かのアクションに付随してデータを入れるか or 独自アクション,ルーティング、API作成、フロントで叩いてもらう)
- （やらない）TOPのとこplaylistもしかしたらfavorite使う可能性がある -->
favorited一応data保存されてるっぽい


watch すーさん
playlist 
Youtubeみたいに配置
ボタン化




<div className={styles.duration}>
  {Math.round(clipData.duration)}秒
</div>

# clg
clg tabキーでconsole.log()が出せるｘ

# imgタグ
div className=style.imgとかにしてさらに中にimgタグを入れると
cssで.img {
  img{

  }
}
とかにして2重で入れ子にする必要がある

# relativeとabsolute
positionプロパティは要素の配置を指定するプロパティで、初期値はstatic

relativeとabsoluteはセットで使う
position: relative; は、基本的に単体で使用することは少ないです。position: absolute; を使いたい場合に、その親要素に対してposition: relative; を適用してセットで使用し

**親にrelative 子にabsolute**

positionプロパティは「top」「right」「bottom」「left」の4つのプロパティと合わせて要素の配置を指定します。4つ全てを使わずに適用することもできますが、
**位置を確定させるために、最低でも2つは適用するようにしましょう。**


子要素にposition: absolute; を適用したことによって、適用した要素が浮いたような状態

positionプロパティはstaticを除き、要素の重なり順を指定するz-indexを適用することができます

.box {
  width: 200px;
  height: 200px;
  background: orange;
  position: relative;
  top: 50px;
  left: 80px;
}

.min-box {
  width: 100px;
  height: 100px;
  background: red;
}

z-indexを指定しない場合は、boxクラス要素はposition: relative; を適用していないmin-boxクラス要素に対して、上に重なるように配置
position側が相対的に上

z-index: -1;をすると裏側に

# positionのrelativeとabsoluteの使い所3選！
positionのrelativeとabsoluteを使う場面としては、次の3つがあります。

- positionで上下左右中央寄せする
- positionで要素を重ねる
- positionとz-indexを組み合わせる

absoluteは
relativeはみんなと関係値で位置決めてるけど
俺は好きなように動くぜ 俺の位置は絶対だぜ
誰にも変えられないぜ


# position のrelativeとabsoluteが効かないときの対処法
position プロパティが効かない場合は下記のような理由が考えられます。

親要素にposition: relative; を適用していない
top, left, bottom, rightなど位置の指定を間違えている
親要素にoverflow: hidden; を適用している
z-indexが負けている
要素に高さやpaddingを指定していない

親要素にheightやpaddingを適用して親要素の高さを保持する

# 背景透過処理
background-color プロパティに rgba(102, 102, 102, 0.7) を使用
最後の0.7の値を変える
濃いと1に近いと透過しない
